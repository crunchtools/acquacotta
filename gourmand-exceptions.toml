# Gourmand Exceptions
# These are intentional patterns, not violations to fix.

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# summary_litter - Legitimate project files, not AI status litter
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[exceptions]]
check = "summary_litter"
path = "CLAUDE.md"
justification = "Claude Code project instructions file - standard AI assistant configuration"

[[exceptions]]
check = "summary_litter"
path = "requirements.txt"
justification = "Standard Python dependency file - pip convention"

[[exceptions]]
check = "summary_litter"
path = "requirements-dev.txt"
justification = "Standard Python dev dependencies file - pip convention"

# Spec-kit feature specification files (RFC/ADR-style workflow)
[[exceptions]]
check = "summary_litter"
path = "specs/001-slidable-timer/spec.md"
justification = "Spec-kit feature specification - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/001-slidable-timer/plan.md"
justification = "Spec-kit implementation plan - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/001-slidable-timer/tasks.md"
justification = "Spec-kit task breakdown - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/001-slidable-timer/research.md"
justification = "Spec-kit research notes - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/001-slidable-timer/data-model.md"
justification = "Spec-kit data model documentation - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/001-slidable-timer/quickstart.md"
justification = "Spec-kit quick start guide - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/001-slidable-timer/checklists/requirements.md"
justification = "Spec-kit requirements checklist - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/007-per-user-storage/spec.md"
justification = "Spec-kit feature specification - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/007-per-user-storage/checklists/requirements.md"
justification = "Spec-kit requirements checklist - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/008-per-user-extensions/spec.md"
justification = "Spec-kit feature specification - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/008-per-user-extensions/checklists/requirements.md"
justification = "Spec-kit requirements checklist - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/009-mandatory-plugins/spec.md"
justification = "Spec-kit feature specification - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/009-mandatory-plugins/checklists/requirements.md"
justification = "Spec-kit requirements checklist - RFC/ADR-style development workflow"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# random_scripts - Intentional entry points
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[exceptions]]
check = "random_scripts"
path = "app.py"
justification = "Main Flask application - standard Python web app entry point"

[[exceptions]]
check = "random_scripts"
path = "static/entrypoint.sh"
justification = "Container entrypoint script for Gunicorn - required for deployment"

[[exceptions]]
check = "random_scripts"
path = "smoke_test.sh"
justification = "Post-deploy smoke test invoked by the deploy process — a documented operational entry point"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# single_use_helpers - Extracted to reduce cyclomatic complexity
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[exceptions]]
check = "single_use_helpers"
path = "app.py"
justification = "Helper functions extracted to satisfy ruff C901 max-complexity=10. Gourmand conflicts with ruff here."

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# verbose_comments - Intentional documentation
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[exceptions]]
check = "verbose_comments"
path = "app.py"
justification = "Comments document Flask routes and complex business logic"

[[exceptions]]
check = "verbose_comments"
path = "sheets_storage.py"
justification = "Comments document Google Sheets API interactions"

[[exceptions]]
check = "verbose_comments"
path = "tests/test_app.py"
justification = "Test comments provide test structure clarity"

[[exceptions]]
check = "verbose_comments"
path = "tests/test_sheets_storage.py"
justification = "Test comments provide test structure clarity"

[[exceptions]]
check = "verbose_comments"
path = "tests/conftest.py"
justification = "Fixture docstrings are required for pytest"

[[exceptions]]
check = "verbose_comments"
path = "static/js/storage.js"
justification = "Comments document IndexedDB operations and sync logic"

[[exceptions]]
check = "verbose_comments"
path = "static/js/utils.js"
justification = "Comments document date/time utility functions"

[[exceptions]]
check = "verbose_comments"
path = "tests/js/test_utils.js"
justification = "Test comments provide test structure clarity"

[[exceptions]]
check = "verbose_comments"
path = "tests/test_timezone.js"
justification = "Test comments provide test structure clarity"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# single_use_helpers - JavaScript domain boundary functions
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[exceptions]]
check = "single_use_helpers"
path = "static/js/storage.js"
justification = "Functions like loadCredentials, calculateReportStats, syncOperationToSheets are distinct domain operations, not fake modularity"

[[exceptions]]
check = "single_use_helpers"
path = "static/js/utils.js"
justification = "snapTo30Minutes and getTodayString are named utility functions with clear single responsibilities"

[[exceptions]]
check = "single_use_helpers"
path = "tests/js/test_utils.js"
justification = "Test helper functions (displayResults, escapeHtml) are standard test infrastructure"

[[exceptions]]
check = "single_use_helpers"
path = "tests/test_timezone.js"
justification = "Individual test functions (testAutomaticTimezone, etc.) are standard test organization, not fake modularity"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# deferred_work - Domain terms, not deferred work markers
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[exceptions]]
check = "deferred_work"
path = "todos_plugin.py"
justification = "This IS a todos plugin — 'todo' in variable/function names are domain terms, not deferred work markers"

[[exceptions]]
check = "deferred_work"
path = "static/js/storage.js"
justification = "storage.js implements the Todos feature — 'Todo' in section headers, variable names, and store references are domain terms, not deferred work markers"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# conditional_wrapper - Legitimate guard clauses
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[exceptions]]
check = "conditional_wrapper"
path = "json_google_drive_storage.py"
justification = "save_pomodoros_batch guard clause avoids needless download/parse/upload cycle on empty input — legitimate API boundary validation"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# MCP server plugin (#111) — new files triaged into the same
# patterns already accepted for app.py and the storage modules.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[exceptions]]
check = "random_scripts"
path = "mcp_server.py"
justification = "Second entry point — the FastMCP server systemd runs alongside Flask, same status as app.py"

[[exceptions]]
check = "deferred_work"
path = "mcp_server.py"
justification = "'todos' refers to the todos plugin it activates — a domain term, not a deferred-work marker"

[[exceptions]]
check = "deferred_work"
path = "app.py"
justification = "'todos' refers to the todos plugin it registers — a domain term, not a deferred-work marker"

[[exceptions]]
check = "deferred_work"
path = "tests/test_app.py"
justification = "'todos' refers to the todos plugin under test — a domain term, not a deferred-work marker"

[[exceptions]]
check = "deferred_work"
path = "tests/test_mcp_plugin_gating.py"
justification = "'todos' refers to the todos plugin under test — a domain term, not a deferred-work marker"

[[exceptions]]
check = "deferred_work"
path = "tests/test_mcp_todos.py"
justification = "'todos' refers to the todos plugin under test — a domain term, not a deferred-work marker"

[[exceptions]]
check = "single_use_helpers"
path = "mcp_server.py"
justification = "_build_drive_service isolates the Google OAuth/Drive boundary; same rationale as app.py's extracted helpers"

[[exceptions]]
check = "verbose_comments"
path = "mcp_server.py"
justification = "Comments document the sealed-token auth and per-user epoch revocation model"

[[exceptions]]
check = "verbose_comments"
path = "plugin_registry.py"
justification = "Comments document the plugin type system, mandatory/optional model, and MCP registrar contract"

[[exceptions]]
check = "verbose_comments"
path = "pomodoro_tools.py"
justification = "Comments document the pomodoro plugin metadata and MCP tool contract"

[[exceptions]]
check = "verbose_comments"
path = "todos_plugin.py"
justification = "Comments document the read-modify-write full-replace contract shared with the web UI's sync path"

[[exceptions]]
check = "verbose_comments"
path = "tests/test_mcp_tokens.py"
justification = "Test comments provide test structure clarity"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# JSON on pCloud storage plugin (#96) — same patterns already
# accepted for the Google Drive storage plugin it mirrors.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[[exceptions]]
check = "conditional_wrapper"
path = "json_pcloud_storage.py"
justification = "save_pomodoros_batch guard clause avoids needless download/parse/upload cycle on empty input — legitimate API boundary validation, identical to json_google_drive_storage.py"

[[exceptions]]
check = "single_use_helpers"
path = "transports/pcloud_transport.py"
justification = "_find_file is the shared stat-and-unwrap step behind download_file and file_exists; it exists to keep the transport interface identical to GoogleDriveTransport, which is what makes the storage plugins interchangeable"

[[exceptions]]
check = "summary_litter"
path = "specs/010-json-pcloud/spec.md"
justification = "Spec-kit feature specification - RFC/ADR-style development workflow"

[[exceptions]]
check = "summary_litter"
path = "specs/010-json-pcloud/plan.md"
justification = "Spec-kit implementation plan - RFC/ADR-style development workflow"
